#include <memory>
#include "gtest/gtest.h"
#include "src/lib/Card.h"

TEST(CardShould, ReturnSomething){
/*
	std::unique_ptr<Card> card = std::make_unique<Card>();
	std::string actual = card->DoSomething();
	std::string expected = Something;
	EXPECT_EQ(expected, actual);
*/
}
